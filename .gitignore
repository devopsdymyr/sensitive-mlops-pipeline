.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# MLflow
mlruns/

# DVC
.dvc/cache
.dvc/tmp

# Feast local state (regenerated by `feast apply`)
data/feast/

# DVC pipeline outputs (do not commit to Git — DVC caches them; Git only has dvc.yaml / dvc.lock)
data/raw/*.csv
data/processed/*.parquet

# Local test outputs
predictions.csv

models/risk_label_classes.json
